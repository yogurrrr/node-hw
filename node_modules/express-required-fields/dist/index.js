"use strict";
var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
var utils_1 = require("./utils");
//accepts an array of strings of the required fields, and an options object
var required = function (fields, opts) {
    if (opts === void 0) { opts = {}; }
    return function (req, res, next) {
        var data = __assign({}, (opts.accept_query ? req.query : {}), (opts.accept_headers ? req.headers : {}), req.body);
        var show_required = opts.show_required != undefined ? opts.show_required : true;
        var show_received = opts.show_received != undefined ? opts.show_received : true;
        fields.every(function (arg) { return Object.keys(data).indexOf(arg) > -1; })
            ? next()
            : res.status(opts.error_status || 400).json(__assign({ message: opts.message || 'Bad Request' }, (show_required ? { required: fields.join(', ') } : {}), (show_received
                ? { received: Object.keys(utils_1.pick.apply(void 0, [data].concat(fields))).join(', ') }
                : {})));
    };
};
module.exports = required;
